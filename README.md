# Online_GP
Five online Gaussian Process algorithms: Full GP, VFE, RGP, SVGP, and SRGP.
Each algorithm is implemented as a class alongside its dataset evaluation code in a single file.
All datasets used for evaluation are located in the `data` folder.
